# /solve — executed next prompt — 2026-09-11

> Treat the physical-device v1 result as a PASS of the native single-capture architecture and a HOLD only on transcript assembly/routing. Do not change the audio architecture. Repair the smallest remaining product defects: eliminate callback-time turn leakage, aggregate all committed STT segments per turn, keep partial hypotheses ephemeral, preserve human locks, and use Android word-level recognition timestamps when the provider supplies them. Fall back to utterance-origin routing when word timing is unavailable. Rebuild an installable APK and stop at the next physical-device JSON gate. Do not touch the qualified browser beta.

Execution target: Android native POC version 0.2.0 on the existing isolated branch.
